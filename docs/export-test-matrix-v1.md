# Export test matrix v1

The export integration suite covers combined grid output, per-clip selection,
PNG and ZIP assembly, standard grid frame JSON, companion metadata, trimmed
source offsets, optional packed edge extrusion, and multipage packed output.
Lower-level suites separately cover deterministic sampling, MaxRects placement,
page composition, and PixiJS reload behavior in Chromium.
